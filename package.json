{
  "name": "present-framework",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install:all": "npm install",
    "cli": "node packages/cli/src/index.js",
    "ui": "PLAN_ROOT=$(pwd) npm run dev --workspace=packages/ui"
  },
  "overrides": {
    "vite": "7.3.1"
  }
}
